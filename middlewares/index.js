const validateBody = require("./validateBody");
const updateBody = require("./updateBody");
const isValidId = require("./isValidId");
const authenticate = require("./authenticate");
const uploud = require("./upload");

module.exports = {
  validateBody,
  updateBody,
  isValidId,
  authenticate,
  uploud,
};